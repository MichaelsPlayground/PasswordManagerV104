<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="de.androidcrypto.passwordmanager">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".AuthenticateActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AddEntryActivity" />
        <activity android:name=".UpdateEntryActivity" />
        <activity android:name=".DatabaseImportClipboard" />
        <activity android:name=".DatabaseImportFile" />
        <activity android:name=".MasterkeyExportClipboard" />
        <activity android:name=".AboutActivity" />
        <activity android:name=".MainActivity"/>
    </application>

</manifest>